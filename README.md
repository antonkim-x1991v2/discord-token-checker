discord token checker

quick script i threw together to check a list of tokens from a text file.

install requirements:
pip install -r requirements.txt

run it:
python checker.py tokens.txt

<!-- last-sync: 2026-09-21 -->
